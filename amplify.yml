version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Installing dependencies..."
    build:
      commands:
        - echo "Building the site..."
  artifacts:
    baseDirectory: v2
    files:
      - '**/*'