version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Installing dependencies..."
        - python scripts/version_calculator.py  # Génère le fichier version.txt
    build:
      commands:
        - echo "Building the site..."
  artifacts:
    baseDirectory: v2
    files:
      - '**/*'
      - ../version.txt  # Assure que version.txt est inclus